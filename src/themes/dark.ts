import { createTheme } from "@mui/material";

export const dark = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#222943",
        },
        secondary: {
            main: "#f16d1d",
        },
    },
});